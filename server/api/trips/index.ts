import supabase from "~/server/db/supabase";

export default defineEventHandler(async (event) => {
  // get all the unconfirmed trips from the database
  // TODO - Implement as a route, instead of using the client.
});